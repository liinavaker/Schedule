/**
 * 
 */
package ee.itcollege.android.schedule;

import android.app.AlertDialog;
import android.widget.EditText;
import android.app.Activity;
import android.graphics.Typeface;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.TextView;

/**
 * @author Heiki
 * 
 */
public class GetUserID {
/*
    @SuppressWarnings("nls")
    public static void onEnterID_clicked(String studentID) {
	EditText userInput = (EditText) findViewById(R.id.IDEntryBox);
	String abc = userInput.getText().toString();

	while (!(abc).equals("bye")) {

	    abc = userInput.getText().toString();// user should enter the text
						 // from keyboard and the while
						 // loop should go and check the
						 // condition.but not able to
						 // enter the text

	    // do some operation with abc

	}

	// create URI from user input
	ScheduleViewerActivity.meetodMidaTahadValjaKutsuda(userInput.getText()
		.toString());

	// replaces hardwired student ID with user entered ID number from OIS
	ScheduleViewerActivity.userID = (userInput.getText().toString());
	// =================================

	AlertDialog.Builder alertDialogBuilder = new AlertDialog.Builder(null);

	// set dialog message
	alertDialogBuilder.setCancelable(true);

	// create alert dialog
	AlertDialog alertDialog = alertDialogBuilder.create();

	// show it
	alertDialog.show();

    }
*/
}
